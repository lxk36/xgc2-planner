name: release

on:
  workflow_dispatch:
    inputs:
      expected_version:
        description: Expected .xgc2/product.yml version
        required: true
        type: string
      expected_source_sha:
        description: Expected source commit SHA
        required: true
        type: string
      publish_apt:
        description: Publish built debs to the XGC2 APT repository
        required: true
        default: false
        type: boolean
      run_cpp_quality:
        description: Run C++ quality checks
        required: true
        default: false
        type: boolean
      run_source_tests:
        description: Run source tests
        required: true
        default: false
        type: boolean

defaults:
  run:
    shell: bash

jobs:
  validate-release-inputs:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.product.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Validate expected version and source SHA
        id: product
        run: |
          set -euo pipefail
          version="$(sed -nE 's|^version:[[:space:]]*([^[:space:]#]+).*|\1|p' .xgc2/product.yml | head -n1)"
          if [[ -z "${version}" ]]; then
            echo "::error::missing version in .xgc2/product.yml"
            exit 1
          fi
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          if [[ "${version}" != "${{ inputs.expected_version }}" ]]; then
            echo "::error::product.yml version ${version} does not match expected_version ${{ inputs.expected_version }}"
            exit 1
          fi
          source_sha="$(git rev-parse HEAD)"
          if [[ "${source_sha}" != "${{ inputs.expected_source_sha }}" ]]; then
            echo "::error::HEAD ${source_sha} does not match expected_source_sha ${{ inputs.expected_source_sha }}"
            exit 1
          fi

  package-compliance:
    needs: validate-release-inputs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check package metadata and packaging consistency
        run: |
          set -euo pipefail
          git diff --check HEAD
          .xgc2/scripts/check_package_compliance.sh

  cpp-quality:
    if: ${{ inputs.run_cpp_quality }}
    needs: validate-release-inputs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run C++ static quality gate
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends clang-format
          .xgc2/scripts/check_cpp_quality.sh

  source-tests:
    if: ${{ inputs.run_source_tests }}
    needs: validate-release-inputs
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Build and run ROS package checks
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/package-tests"
          docker run --rm \
            -e DEBIAN_FRONTEND=noninteractive \
            -e ROS_PACKAGE_CHECK_WORK_DIR=/workspace/work \
            -v "$GITHUB_WORKSPACE:/workspace/repo:ro" \
            -v "$RUNNER_TEMP/package-tests:/workspace/work" \
            ros:noetic-ros-base-focal \
            bash -lc '
              set -euo pipefail
              apt-get update
              apt-get install -y --no-install-recommends \
                build-essential \
                cmake \
                libboost-dev \
                libeigen3-dev \
                libompl-dev \
                libyaml-cpp-dev \
                rsync \
                ros-noetic-geometry-msgs \
                ros-noetic-message-generation \
                ros-noetic-nav-msgs \
                ros-noetic-roscpp \
                ros-noetic-rospack \
                ros-noetic-sensor-msgs \
                ros-noetic-std-msgs \
                ros-noetic-visualization-msgs
              /workspace/repo/.xgc2/scripts/check_ros_packages.sh --work-dir /workspace/work
            '

  deb-build:
    needs: validate-release-inputs
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
        planner_group: [common, gcopter, jps3d, cerlab-planner, intent-mpc, mader, robust-mader, ego-planner, fast-planner]
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    env:
      PACKAGE_VERSION: ${{ needs.validate-release-inputs.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Build planner deb packages in Docker
        run: |
          .xgc2/scripts/build_debs_in_docker.sh \
            --planner-group "${{ matrix.planner_group }}" \
            --work-dir "$RUNNER_TEMP/planner-${{ matrix.planner_group }}-${{ matrix.arch }}" \
            --output-dir "$GITHUB_WORKSPACE/debs" \
            --skip-install-check

      - name: Upload deb artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xgc2-planner-${{ matrix.planner_group }}-release-debs-${{ matrix.arch }}
          path: debs/*.deb

  meta-build:
    needs: validate-release-inputs
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    env:
      PACKAGE_VERSION: ${{ needs.validate-release-inputs.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Build planner metapackages
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends fakeroot
          mkdir -p debs
          .xgc2/scripts/package_debs.sh \
            --package-group meta \
            --output-dir "$GITHUB_WORKSPACE/debs"

      - name: Upload meta deb artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xgc2-planner-meta-release-debs-${{ matrix.arch }}
          path: debs/*.deb

  publish-apt:
    if: ${{ inputs.publish_apt }}
    needs:
      - deb-build
      - meta-build
    runs-on: ubuntu-latest
    env:
      APT_REPO_HOST: ${{ secrets.APT_REPO_HOST }}
      APT_REPO_PORT: ${{ secrets.APT_REPO_PORT }}
      APT_REPO_SSH_KEY: ${{ secrets.APT_REPO_SSH_KEY }}
      APT_REPO_KNOWN_HOSTS: ${{ secrets.APT_REPO_KNOWN_HOSTS }}
    steps:
      - uses: actions/checkout@v4

      - name: Download deb artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: xgc2-planner-*-release-debs-*
          path: debs
          merge-multiple: true

      - name: Publish to self-hosted APT repository
        run: |
          set -euo pipefail
          test -n "${APT_REPO_HOST}"
          test -n "${APT_REPO_SSH_KEY}"
          test -n "${APT_REPO_KNOWN_HOSTS}"
          export APT_REPO_PORT="${APT_REPO_PORT:-22}"
          .xgc2/scripts/publish_apt_repo.sh --deb-dir debs
